# Program to store student records (Name, Marks)
# The list is mutable, but the student details (tuples) are fixed
records = [
    ("Hafiya", 95),
    ("Ali", 88),
    ("Sara", 92)
]

print("Total Records:", len(records))
print("First Student Record:", records[0])
print("Name of Second Student:", records[1][0])
