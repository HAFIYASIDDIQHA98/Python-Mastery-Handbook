# Calculating Slope (m) for a simple line y = mx + c
x = [1, 2, 3, 4, 5]
y = [2, 4, 6, 8, 10]

# Simple slope calculation: (y2-y1)/(x2-x1)
slope = (y[1] - y[0]) / (x[1] - x[0])

print(f"X values: {x}")
print(f"Y values: {y}")
print(f"Calculated Slope: {slope}")
